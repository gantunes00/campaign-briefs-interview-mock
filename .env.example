# Required. Candidates use: Authorization: Bearer <this value>
INTERVIEW_API_TOKEN=change-me-in-production

# Optional override (Cloud Run sets PORT automatically)
# PORT=8080

# Optional: base URL for tests/campaign-briefs.api.example.test.mjs (include /v1)
# API_BASE=http://localhost:8080/v1
